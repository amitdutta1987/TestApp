<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
      Cloud sync. The app remains offline-first — every sale and stock movement is
      written to local SQLite first and works with no signal at all — but it now
      pushes those changes to the sync Worker and pulls other devices' changes
      back, and fetches product photos from object storage.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Lets the app skip a sync attempt it knows will fail, rather than waiting
         for a request to time out on the scan-to-sell path. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Live barcode scanning -->
    <uses-permission android:name="android.permission.CAMERA" />

    

    <!-- Reading product photos from the gallery (Android 13+ scoped media) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        android:minSdkVersion="33" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

     <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <!--
        Lets us hand Excel exports and backup ZIPs to other apps (Drive, Gmail,
        WhatsApp, Sheets) without requesting any storage permission.
      -->
      <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.provider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_provider_paths" />
      </provider>

      <!-- Bundles the ML Kit barcode model into the APK so detection works offline. -->
      <meta-data
        android:name="com.google.mlkit.vision.DEPENDENCIES"
        android:value="barcode" />
    </application>
</manifest>
